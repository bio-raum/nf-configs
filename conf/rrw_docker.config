params {
	reference_base = "$HOME/NGS/nextflow_refs"
}

process {
	executor = 'local'
	resourceLimits = [ cpus: 8, memory: 64.GB, time: 72.h ]
}

executor {
	queueSize=5
}

docker {
    enabled = true
    cacheDir = "$HOME/NGS/nextflow_envs"
}
